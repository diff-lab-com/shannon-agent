name: cost_below_over_budget
description: "Negative behavioral assertion: costs exceeding the ceilings must fail for both task and turn bases"
prompt: "Read src/main.rs then replace Hello with Goodbye"
provider: anthropic

setup:
  files:
    - path: src/main.rs
      content: |
        fn main() {
            println!("Hello, world!");
        }

mock_responses:
  - response:
      type: tool_use
      tool: Read
      tool_id: toolu_1
      input: { "path": "src/main.rs" }
  - response:
      type: tool_use
      tool: Edit
      tool_id: toolu_2
      input: { "path": "src/main.rs", "old_string": "Hello", "new_string": "Goodbye" }
  - response:
      type: text
      content: "Updated the greeting."

validate:
  - rule: cost_below
    max_usd: 0.015
    per: task
  - rule: cost_below
    max_usd: 0.015
    per: turn
