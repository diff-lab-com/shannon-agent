name: text_only
description: "Simple text response with no tool use"
prompt: "What is 2+2?"
provider: anthropic

setup:
  files: []

mock_responses:
  - response:
      type: text
      content: "2+2 equals 4."

validate:
  - rule: exit_code
    value: success
