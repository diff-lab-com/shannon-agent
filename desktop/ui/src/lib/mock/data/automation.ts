// Automation mock data: scheduled routines, triggered routines, hook events, profiles.
import type {
  ScheduledRoutine,
  TriggeredRoutineDto,
  HookEventInfo,
  ProfilesList,
  CustomProfileInfo,
} from '@/types'

const now = Date.now()

export const MOCK_SCHEDULED_ROUTINES: ScheduledRoutine[] = [
  {
    id: 'sched-001',
    name: 'weekly-metrics-digest',
    description: 'Every Monday 9am — compile key metrics into a digest and post to #ops',
    trigger_type: 'cron' as never,
    trigger: { cron: '0 9 * * 1' },
    command: 'Compile weekly metrics digest and post to #ops',
    enabled: true,
    last_run_at: Math.floor((now - 3 * 86400_000) / 1000),
    next_run_at: Math.floor((now + 4 * 86400_000) / 1000),
    last_status: 'succeeded',
    execution_policy: {
      max_retries: 2,
      timeout_secs: 600,
      notify_on_failure: true,
      auto_archive_when_empty: false,
      result_routing: ['slack:#ops'],
    },
  } as unknown as ScheduledRoutine,
  {
    id: 'sched-002',
    name: 'nightly-backup-check',
    description: 'Every day at 2am — verify last night\'s backup completed',
    trigger_type: 'cron' as never,
    trigger: { cron: '0 2 * * *' },
    command: 'Check backup status and alert if failed',
    enabled: true,
    last_run_at: Math.floor((now - 86400_000) / 1000),
    next_run_at: Math.floor((now + 86400_000) / 1000),
    last_status: 'succeeded',
    execution_policy: {
      max_retries: 3,
      timeout_secs: 120,
      notify_on_failure: true,
      auto_archive_when_empty: false,
      result_routing: ['notification', 'log'],
    },
  } as unknown as ScheduledRoutine,
  {
    id: 'sched-003',
    name: 'stale-pr-warn',
    description: 'Every 4 hours — list PRs open > 48h with no activity',
    trigger_type: 'interval' as never,
    trigger: { interval_secs: 14400 },
    command: 'Find stale PRs (>48h, no activity) and post to #eng',
    enabled: true,
    last_run_at: Math.floor((now - 7200_000) / 1000),
    next_run_at: Math.floor((now + 7200_000) / 1000),
    last_status: 'succeeded',
    execution_policy: {
      max_retries: 1,
      timeout_secs: 60,
      notify_on_failure: false,
      auto_archive_when_empty: true,
      result_routing: ['slack:#eng'],
    },
  } as unknown as ScheduledRoutine,
  {
    id: 'sched-004',
    name: 'customer-health-scan',
    description: 'Daily 8am — scan health scores, flag accounts < 60',
    trigger_type: 'cron' as never,
    trigger: { cron: '0 8 * * *' },
    command: 'Scan customer health scores; flag any < 60 to CSM channel',
    enabled: false,
    last_run_at: Math.floor((now - 2 * 86400_000) / 1000),
    next_run_at: null,
    last_status: 'failed',
    execution_policy: {
      max_retries: 3,
      timeout_secs: 300,
      notify_on_failure: true,
      auto_archive_when_empty: false,
      result_routing: ['slack:#csm', 'email:csm@company.com'],
    },
  } as unknown as ScheduledRoutine,
  {
    id: 'sched-005',
    name: 'release-notes-draft',
    description: 'Every Friday 4pm — draft release notes from merged PRs',
    trigger_type: 'cron' as never,
    trigger: { cron: '0 16 * * 5' },
    command: 'Draft release notes from this week\'s merged PRs',
    enabled: true,
    last_run_at: Math.floor((now - 5 * 86400_000) / 1000),
    next_run_at: Math.floor((now + 2 * 86400_000) / 1000),
    last_status: 'succeeded',
    execution_policy: {
      max_retries: 1,
      timeout_secs: 600,
      notify_on_failure: true,
      auto_archive_when_empty: false,
      result_routing: ['slack:#product', 'notification'],
    },
  } as unknown as ScheduledRoutine,
  {
    id: 'sched-006',
    name: 'webhook-incoming',
    description: 'Triggered by Stripe webhook on invoice.paid',
    trigger_type: 'webhook' as never,
    trigger: { webhook_id: 'wh_stripe_invoice', event: 'invoice.paid' },
    command: 'Send thank-you email and update CRM',
    enabled: true,
    last_run_at: Math.floor((now - 3600_000) / 1000),
    next_run_at: null,
    last_status: 'succeeded',
    execution_policy: {
      max_retries: 5,
      timeout_secs: 30,
      notify_on_failure: true,
      auto_archive_when_empty: false,
      result_routing: ['log'],
    },
  } as unknown as ScheduledRoutine,
]

export const MOCK_TRIGGERED_ROUTINES: TriggeredRoutineDto[] = [
  {
    name: 'auto-lint-after-edit',
    trigger: 'PostToolUse',
    command: 'cargo clippy --workspace',
    matcher: 'tool=Write|Edit',
    pattern: '**/*.rs',
    description: 'Run clippy automatically after Rust file edits',
    enabled: true,
    last_fired_at: Math.floor((now - 600_000) / 1000),
    fire_count: 142,
  } as unknown as TriggeredRoutineDto,
  {
    name: 'notify-on-task-failed',
    trigger: 'TaskCompleted',
    command: 'notify-task-failed',
    matcher: 'status=failed',
    pattern: '',
    description: 'Notify on any failed task',
    enabled: true,
    last_fired_at: Math.floor((now - 4 * 3600_000) / 1000),
    fire_count: 8,
  } as unknown as TriggeredRoutineDto,
  {
    name: 'snapshot-before-edit',
    trigger: 'PreToolUse',
    command: 'git stash create',
    matcher: 'tool=Edit|Write',
    pattern: '',
    description: 'Snapshot git state before file edits for undo',
    enabled: true,
    last_fired_at: Math.floor((now - 90_000) / 1000),
    fire_count: 89,
  } as unknown as TriggeredRoutineDto,
  {
    name: 'welcome-new-session',
    trigger: 'SessionStart',
    command: 'echo "Welcome back!"',
    matcher: '',
    pattern: '',
    description: 'Print welcome on new session',
    enabled: false,
    last_fired_at: null,
    fire_count: 0,
  } as unknown as TriggeredRoutineDto,
]

export const MOCK_HOOK_EVENTS: HookEventInfo[] = [
  { event: 'PreToolUse', description: 'Before any tool is called', registered: true, fire_count: 1240 },
  { event: 'PostToolUse', description: 'After any tool completes', registered: true, fire_count: 1240 },
  { event: 'SessionStart', description: 'When a new session begins', registered: true, fire_count: 87 },
  { event: 'SessionEnd', description: 'When a session ends', registered: false, fire_count: 0 },
  { event: 'TaskCreated', description: 'A new task is created', registered: true, fire_count: 56 },
  { event: 'TaskCompleted', description: 'A task finishes (any status)', registered: true, fire_count: 51 },
  { event: 'TaskBlocked', description: 'A task becomes blocked', registered: true, fire_count: 12 },
  { event: 'AgentStart', description: 'A sub-agent is spawned', registered: true, fire_count: 34 },
  { event: 'AgentStop', description: 'A sub-agent terminates', registered: true, fire_count: 32 },
  { event: 'PermissionRequest', description: 'User is asked for tool permission', registered: true, fire_count: 218 },
  { event: 'QueryStart', description: 'A user message starts processing', registered: false, fire_count: 0 },
  { event: 'QueryComplete', description: 'A query finishes streaming', registered: false, fire_count: 0 },
  { event: 'QueryFailed', description: 'A query errored out', registered: true, fire_count: 4 },
  { event: 'FileChanged', description: 'A watched file changed', registered: true, fire_count: 412 },
  { event: 'McpServerConnected', description: 'An MCP server connected', registered: false, fire_count: 0 },
  { event: 'McpServerDisconnected', description: 'An MCP server disconnected', registered: false, fire_count: 0 },
  { event: 'RoutineFired', description: 'A triggered routine fired', registered: true, fire_count: 233 },
  { event: 'RoutineFailed', description: 'A triggered routine errored', registered: true, fire_count: 6 },
  { event: 'ConfigChanged', description: 'Configuration was modified', registered: false, fire_count: 0 },
  { event: 'WorktreeCreate', description: 'A git worktree was created', registered: true, fire_count: 18 },
  { event: 'WorktreeRemove', description: 'A git worktree was removed', registered: true, fire_count: 15 },
] as unknown as HookEventInfo[]

export const MOCK_PROFILES: ProfilesList = {
  current: 'standard',
  builtin: [
    { name: 'standard', description: 'Default balanced permission profile', source: 'builtin' },
    { name: 'relaxed', description: 'Broader auto-approve, fewer prompts', source: 'builtin' },
    { name: 'strict', description: 'Confirm everything except read-only', source: 'builtin' },
    { name: 'full-auto', description: 'Auto-approve all non-destructive', source: 'builtin' },
  ],
  custom: [
    {
      name: 'research-mode',
      description: 'For research sessions — broad read access, no writes',
      auto_approve: ['read_file', 'search', 'list_directory', 'web_search'],
      confirm: ['write_file', 'bash'],
      deny: ['rm_rf', 'git_push', 'git_reset_hard'],
    } as unknown as CustomProfileInfo,
    {
      name: 'demo-mode',
      description: 'For demos — auto-approve common safe tools',
      auto_approve: ['read_file', 'write_file', 'search', 'list_directory', 'bash'],
      confirm: ['git_push', 'git_reset_hard'],
      deny: ['rm_rf'],
    } as unknown as CustomProfileInfo,
  ],
} as unknown as ProfilesList
