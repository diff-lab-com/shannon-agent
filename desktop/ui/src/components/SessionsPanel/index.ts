// Re-export entry point so consumers can `import { SessionsPanel } from
// '@/components/SessionsPanel'` without needing to know about the
// underlying file path.
export { SessionsPanel } from './SessionsPanel'
export type { SessionsPanelProps } from './SessionsPanel'
