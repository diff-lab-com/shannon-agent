基于刚完成的 UI/UX 综合评审(发现 2 个 P0、9 个 P1、5 个 P2、7 个产品/旅程缺口),建议按以下顺序执行:

**第 1 步:落盘评审报告**
- 将完整评审写入 `docs/reviews/2026-09-03-ui-ux-review.md`(遵循本仓库 docs 不入库惯例,保持 untracked)

**第 2 步:第一批 P0/速赢修复(小 PR 序列,每个都遵守仓库门禁:lint 0 警告、vitest+Playwright 绿、i18n en+zh 同 commit、每 PR ≥3 主题视觉核对)**
1. 修复 ember/slate 主题 scheme 注册错误(`ThemeContext.tsx:35-36` vs `index.css:610-715`),并把 `e2e/themes.spec.ts` 改为按 `--background` 亮度与注册模式一致的自动校验
2. 修复或移除 `mod+/` 死快捷键(`useKeyboardShortcuts.ts:30-33` 切换不存在的 `.collapsed` 类)
3. AppContext 初始化失败上移为页面级 ErrorState/toast + 重试(不再只 console.warn)
4. TurnTimeline 成本曲线改第二轴/归一化,消除误导性贴地直线;USD 硬编码加注释或提为常量
5. 修复 Welcome env-key 死胡同(env key 可用但非推荐 provider 时允许前进)+ 移除 `TASKS.find(...)!` 非空断言
6. 修复 2 处用户可见英文硬编码(`Markdown.tsx:80`、`MessageBubble.tsx:178`,en+zh 同 commit)

**第 3 步(需你确认优先级后再做):专项与产品级**
- 逐主题对比度 + 全页 axe 专项;聊天列 prop drilling 收敛;DAG/Mermaid 主题感知颜色;轮询→事件(需后端补事件);eval 桌面/CLI 入口(J7);/rewind 桌面入口 + 权限 scope;路由/双入口 IA 清理
- 运行时视觉走查:`pnpm demo` + Playwright 截图,对 Settings/OPC/Memory/Extensions/TurnTimeline 逐页评审

如只需要评审报告本身(不执行修复),第 1 步完成后即可停止。