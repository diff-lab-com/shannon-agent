name: Benchmark Regression

on:
  pull_request:
    branches: [main, dev]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  bench-regression:
    name: Performance Regression Check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.88"
      - uses: Swatinem/rust-cache@v2

      # Run E2E latency tests (threshold-based assertions)
      - name: E2E Latency Tests
        run: cargo test --test e2e_latency_tests -- --test-threads=1

      # Run performance regression tests (threshold-based assertions)
      - name: Performance Regression Tests
        run: cargo test --test perf_regression -- --test-threads=1

      # Run criterion benchmarks and save baseline
      - name: Run Benchmarks
        run: |
          cargo bench --bench query_engine -- --save-baseline pr
          cargo bench --bench core_benchmarks -- --save-baseline pr
          cargo bench --bench recording_benchmarks -- --save-baseline pr
          cargo bench --bench compact_bench -- --save-baseline pr
          cargo bench --bench context_budget_bench -- --save-baseline pr
        continue-on-error: true

      # Run scenario tests (validates YAML scenario pipeline)
      - name: Scenario Runner Tests
        run: cargo test --test scenario_runner_tests -- --test-threads=1

      # Summary
      - name: Benchmark Summary
        if: always()
        run: |
          echo "## Benchmark Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All threshold-based tests passed. See individual step logs for details." >> $GITHUB_STEP_SUMMARY
