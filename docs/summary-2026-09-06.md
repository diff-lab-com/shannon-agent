# Shannon Agent Eval & Improvement Loop — Final Summary

- Date: 2026-09-06
- Worktree/branch: `feat/agent-eval-bench` (merged into local dev)
- Anchor: glm-5.3-flash @ zhipu-coding-plan, app 0.11.0 era
- All eval runs preserved under `~/.shannon/eval/`

## 1. Headline numbers

| 评测 | n | resolved | token in | 备注 |
|---|---|---|---|---|
| 回归池（内部 L1） | 10 × 3 | 旧 3 · flaky 1 · sf 6 → **改进 6/10** | 0.5M | n=1 quick verify, 三个靶向任务救回 |
| SWE-bench Verified 50-pin | 50 × 1 | 旧 30 → **改进 37** | 旧 42M · 改进 34M | 净 +7，4 题是 minimax 从未解出；minimax-m3 同期最佳 33/50 |
| Terminal-Bench 9-pin | 9 × 3 | **3/9 稳定** | 11M | agent 层 3 败 + infra 2 败已修 |
| Terminal-Bench 2.1 全量（harbor） | 89 × 1 | **36/89 = 40.4%** | – | GLM 官方同模型 Claude Code harness：69.2/89 = 77.8% |

## 2. Improvements shipped (P3)

A1 think-only 续推 nudge · A2 系统提示验证指引 · A3 工具输出路径别名 ·
A4 Bash 安全器分级 + 整改建议 · A5 内容级流停滞看门狗 · B3 Write/Bash 根集合统一 ·
B1/B2 判分修正（回归池）· B4 SWE hint 增强 · reg_05 隐藏契约显式化。

## 3. Infrastructure fixes

- swe-harness env-prefix bug（`e092b6b4`，Phase B 引入）
- TB harness 冷路径 + rc=4 重试 + compose 日志（`5285dc0f`/`a7828541`）
- bench_runner `--task-list` shard filter（`6bebe579`）

## 4. Key findings (P2)

- 回归池 6 个 stable_fail 中 **17/18 次 agent 已写对修复**——失败来自系统提示强令跑测试 +
  无工具链、Read/Edit 回显宿主路径而 Bash 是 `/workspace`、Bash 安全器一刀切拒绝展开、
  edit 层 6 轮零余量。
- minimax 历史杠杆：b7→b8 唯一统计显著跳变 (+10 z=2.01) 来自 6 行提示；batch11 17 失败中
  9 个脚手架责任（含 think-only 牺牲 3 题）。
- SWE 跨模型消融：GLM 解出了 3 道 minimax 硬核题（sympy-13031、django-11433、xarray-2905）；
  改进版又额外解出 4 道 minimax 不可的 seaborn-3069/django-10914/astropy-14182/pytest-10051。
- sympy-13031 回归根因：sweep 是基础设施（请求超时→空 patch），down3 0/3 是模型策略方差
  （row_join 零尺寸陷阱），A1/A3/A4 全部排除；新增 backlog A7 引擎重试。

## 5. Open backlog

- **A7** 引擎层 query-failed/context_overflow/rate_limited run 级重试；空 patch 标记 infra
- **C1** events.jsonl turn 字段全程恒为 1（真实 turn 计数靠 request/header 解析）
- **C2** result.json 缺 verify_script rule_outcomes 回写
- **C3** verdict 层 token 回取失败（部分路径 0-token resolved）
- **C4** glm-5.3-flash catalog 参考价（成本列当前不可信）
- **A2 措辞微调**：patch 共享基类方法时追踪子类 override

## 6. Files of record

- `docs/agent-eval-plan-2026-09.md` — 实施方案
- `docs/eval-findings-2026-09-glm.md` — P2 发现与分桶（含逐题证据）
- `~/.shannon/eval/shards/old-baseline-matrix.json` — 50 题旧基线
- `~/.shannon/eval/shards/improved-matrix.json` — 50 题改进基线
- `~/.shannon/eval/v3-glm-tb9/` — TB9 三轮结果
- `~/.shannon/eval/harbor-jobs/jobs/2026-09-06__09-21-13/` — TB2.1 89 题结果

## 7. Post-summary additions (2026-09-07)

- **Second improvement wave landed**: A7 run-retry, C1 turn field, C2 contract test,
  C4 catalog pricing (fixed ~60x cost inflation), musl build, RunBackground tools,
  AnalyzeImages batch, provider A/B harness, ensure-images guard, harbor adapter
  libc-probe/retry/prompt fixes — see docs/backlog.md for the full ledger.
- **TB2.1 acceptance runs v2/v3 were INVALID** (config failures: prompt-path bug,
  watchdog below thinking-silence, 6-way rate limits, over-eager wrap-up prompt).
  Honest status: TB2.1 improvement effect UNVERIFIED; SWE-bench 50 (37 vs 30,
  same-config) remains the accepted evidence. Re-test procedure in docs/backlog.md T1.
- **A2 wrap-up clause reverted** (correctness-first rewrite, f6bac100): it was
  eval-motivated and demonstrably harmful (20 control regressions). B.7 strikes
  auto-enable demoted to opt-in. Full audit: docs/backlog.md §一 L4 / §五.
- Methodology rules institutionalized in docs/backlog.md §一: single-variable
  eval changes; watchdog threshold > model max thinking silence; assert start-event
  prompt field; no prompt changes from single-task evidence.

## 8. Citations

Any published number must carry (n, date, anchor=model·provider·profile); only stable
buckets are citable; n=1 is internal-only.
