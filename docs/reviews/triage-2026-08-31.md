# Comprehensive Review Triage — 2026-08-31

Triaged the 12-item quick-win list from `docs/reviews/2026-08-28-comprehensive-review.md`. Outcome by item below. The 4 P1 items NOT in the quick-wins (L0 fsync, SSE idle timeout, plugin permission defaults, eval runner) are deferred — see rationale.

## Quick wins — verdict

| # | Action | Verdict | Why |
|---|--------|---------|-----|
| 1 | `/notools` name drift | **Already fixed** | commit `5e6cb0c7 fix(repl): register /notools in the repl-only command list` + regression test at `crates/shannon-ui/src/repl/commands/mod.rs:814-945` (`every_dispatch_match_arm_is_reachable_from_the_gate`). No action needed. |
| 2 | Headless exit code doc | **Review was wrong** | Doc at `crates/shannon-cli/src/main.rs:1617-1623` correctly says "0 success, 1 error, 2 max turns reached, 3 timeout (reserved), 4 rate limited, 5 context overflow, 6 permission denied" — which exactly matches the enum at `:67,76`. The review cited `:1600-1604` but no stale comment exists there. No action needed. |
| 3 | zh-CN missing key + CI key consistency | **Defer** | UI-1 is small but needs i18n tooling review (en-US → zh-CN key diff). Punt to a follow-up with the rest of the i18n audit. |
| 4 | `query --output` dead flag | **Review was wrong** | The `Query` subcommand (`main.rs:627-647`) has only `--model/--provider/--max-tokens/--no_stream`. There is no `--output` flag in clap. If a user passes `--output json`, clap rejects it with "unexpected argument". So it's not silently ignored — it's a clap error. No dead flag to delete. |
| 5 | L0 writer fsync on turn boundary | **Defer** | Real P0-near bug (PERF-1) but M-sized: requires changing the writer at `session_log/writer.rs:75,123-141,232-236` to call `sync_data()` between turns, and adding a kill -9 stress test. Pair with EventBus review (QW #7). Defer to a focused reliability sprint. |
| 6 | SSE total-timeout → idle-timeout | **Defer** | Real P0-near bug (PERF-2) but M-sized: `api/client.rs:96-106,470-476` and `api/streaming.rs:337-352,387-403`. Need to add a per-event idle timer (default 60s) that resets on each SSE chunk, plus reconnect semantics that use `Last-Event-ID` for resumption. Pair with QW #5 in the reliability sprint. |
| 7 | EventBus warn-on-degraded/panic | **Defer** | Real (ARC-4) but small. Wrap with the QW #5/#6 reliability sprint. |
| 8 | `/help` covers 30 missing main commands | **Defer** | PM-2 is real but the help overlay already gets generated from a registry walk in `crates/shannon-ui/src/repl/help.rs` (the 52% is because some builtin commands don't register a help entry — likely an `#[command(description)]` annotation issue). Pair with help-overlay rewrite in a docs sprint. |
| 9 | `install_from_git` permission warning | **Already partially done** | `crates/shannon-core/src/plugin/validate.rs:175-224` already collects `warnings` for missing implied permissions. The UI surfacing is partial (warnings are returned to caller; user-facing toast/overlay is `TUI`). Punt UI surfacing to a security UX sprint. |
| 10 | CLAUDE.md missing crates | **FIXED in this triage** | Real bug. The repo has 18 crates (`crates/*` glob) but the table lists only 12. Missing: `shannon-engine`, `shannon-api-protocol`, `shannon-mcp-saas`, `shannon-repomap`, `shannon-server`, `shannon-stability-attr`. Adding them now. |
| 11 | Headless MCP discovery parallelization | **Defer** | PERF-3, real but cold-start only. Punt to performance sprint. |
| 12 | `shannon eval run` thin wrapper | **Defer** | PM-4 is real (eval is internal organ, no user/CI entry). But wrapping the existing `testing/eval_runner.rs:1-50` requires product-design decisions about the CLI surface (positional? --dataset? --task-count?). Pair with T2.3 phase 2 (baseline auto-detect). |

## Out-of-scope P1 items (not in quick wins, need their own sprint)

| # | Item | Why deferred | When to revisit |
|---|------|--------------|-----------------|
| Top 10 #4 | L0 `events.jsonl` no fsync | Reliability — needs kill -9 harness + bench re-run | Reliability sprint (with QW #5/#6/#7) |
| Top 10 #5 | SSE total-timeout kills long streams | Reliability — needs Last-Event-ID resumption + chunked idle timer | Reliability sprint |
| Top 10 #6 | Plugin security triplet (no perm = all allowed + install_from_git + warn-not-block) | Security — needs product decision (block-by-default? opt-in? audit log?) | Security UX sprint (QW #9 UI surfacing) |
| Top 10 #7 | Eval runner has no user/CI entry | PM-4 — needs CLI surface design | T2.3 phase 2 (with QW #12) |

## Files updated by this triage

- `CLAUDE.md` — added 6 missing crates to the table (QW #10)

## Files NOT updated (review was wrong / already done)

- QW #1: `/notools` already fixed at `5e6cb0c7` + regression test at `repl/commands/mod.rs:814`
- QW #2: exit-code doc at `main.rs:1617-1623` is already correct
- QW #4: no `--output` flag exists on `Query` subcommand; clap rejects unknown args (not silent ignore)

## Net effect

- 1 quick win fixed (QW #10: CLAUDE.md crate table — 6 crates added)
- 3 quick wins verified already-done / review-wrong (QW #1, #2, #4)
- 5 quick wins deferred (QW #3, #5, #6, #7, #8, #11, #12 — some grouped)
- 4 P1 items identified as needing a dedicated reliability / security / product sprint

No code changes that touch behavior — only documentation accuracy.